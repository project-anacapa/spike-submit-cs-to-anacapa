
# Please copy env.sh.SAMPLE to env.sh (which is .gitignore'd)
# and then do . env.sh to define the env variables

#for github.ucsb.edu - with only repo access
export GITHUB_PERSONAL_ACCESS_TOKEN=thisIsFakeData
